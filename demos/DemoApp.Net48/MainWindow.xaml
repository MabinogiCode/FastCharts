<Window x:Class="DemoApp.Net48.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:FastCharts.Wpf.Controls;assembly=FastCharts.Wpf"
        mc:Ignorable="d"
        Title="FastCharts Demo (.NET 4.8)" Height="1200" Width="1800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Barre commandes -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="8" Background="#222" >
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Demo .NET 4.8" Foreground="White" FontWeight="Bold" Margin="0,0,16,0" />
                <Button Content="Toggle Theme" Command="{Binding ToggleThemeCommand}" Margin="0,0,8,0" />
                <Button Content="Add Random Series" Command="{Binding AddRandomSeriesCommand}" Margin="0,0,16,0" />
                
                <!-- Chart Selection -->
                <Border Background="White" BorderBrush="Orange" BorderThickness="2" CornerRadius="3" Padding="4" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🎯 Selected:" VerticalAlignment="Center" Margin="0,0,4,0" FontWeight="Bold" Foreground="DarkBlue"/>
                        <ComboBox ItemsSource="{Binding Charts}" 
                                  SelectedItem="{Binding SelectedChart}"
                                  DisplayMemberPath="Title"
                                  Width="120" Margin="2"
                                  ToolTip="Select chart for export operations"/>
                    </StackPanel>
                </Border>
                
                <!-- P1-EXPORT-PNG: Export Controls -->
                <Border Background="LightBlue" BorderBrush="Navy" BorderThickness="1" CornerRadius="3" Padding="4" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾 Export:" VerticalAlignment="Center" Margin="0,0,4,0" FontWeight="Bold" Foreground="Navy"/>
                        <Button Content="📁 Save PNG" Command="{Binding ExportSelectedChartCommand}" Margin="2"
                                ToolTip="Export selected chart to PNG file"/>
                        <Button Content="📋 Copy" Command="{Binding CopySelectedChartToClipboardCommand}" Margin="2"
                                ToolTip="Copy selected chart to clipboard"/>
                        <Button Content="📂 Export All" Command="{Binding ExportAllChartsCommand}" Margin="2"
                                ToolTip="Export all charts to PNG files"/>
                    </StackPanel>
                </Border>
                
                <TextBlock Text="Theme:" Foreground="White" VerticalAlignment="Center" />
                <TextBlock Text="{Binding SelectedTheme}" Foreground="Orange" FontWeight="Bold" Margin="4,0,16,0" />
            </StackPanel>
        </DockPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <UniformGrid Rows="4" Columns="4" Margin="16" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <controls:FastChart Margin="8" Model="{Binding Charts[0]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[1]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[2]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[3]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[4]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[5]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[6]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[7]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[8]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[9]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[10]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[11]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[12]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[13]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[14]}" />
                <controls:FastChart Margin="8" Model="{Binding Charts[15]}" />
            </UniformGrid>
        </ScrollViewer>
    </Grid>
</Window>
